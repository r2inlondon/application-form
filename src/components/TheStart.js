import React from "react";

const TheStart = (props) => (
  <div className="the-end">
    <p>Start Application</p>
    <button onClick={props.TheStart}>Submit Form</button>
  </div>
);

export default TheStart;
